export default function menuLinks() {
    // const navLinks = Array.from(document.querySelectorAll('.page-header__menu-nav-link'));

    // const filteredLinks = navLinks.filter(link => {
    //     const submenu = link.nextElementSibling;
    //     return submenu && submenu.matches('.page-header__submenu-wrapper');
    // });

    // filteredLinks.forEach((link, linkIndex) => {
    //     link.addEventListener('click', event => {
    //         event.preventDefault();
    //         filteredLinks.forEach(link => link.parentElement.classList.remove('active'));
    //         link.parentElement.classList.add('active');
    //     })
    // })

    
}
